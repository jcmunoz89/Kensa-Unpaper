rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny-all until Firestore integration is implemented.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
